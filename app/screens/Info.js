import React from 'react';
import { View } from 'react-native';

const styles = {
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'white'
  }
};

const Info = () => <View style={styles.container} />;

export { Info };
